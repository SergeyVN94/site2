include ../input-toggle/input-toggle.pug
include ../text/text.pug

mixin toggle-button(text = '', checked = false)
    .toggle-button
        +input-toggle(checked)
        .toggle-button__text
            +text({
                text,
            })
