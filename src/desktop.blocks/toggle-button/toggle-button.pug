include ../input-toggle/input-toggle.pug
include ../text/text.pug

mixin toggle-button(options={})
    -
        const {
           text = '',
           checked = false,
        } = options;

    .toggle-button
        +input-toggle({
            checked,
        })
        .toggle-input__text
            +text({
                text,
            })