include /menu/menu.pug

mixin footer-nav(options = {})
    -
        const {
            title = "",
            items = [],
        } = options;

    .footer-nav
        p.footer-nav__title #{title}
        .footer-nav__menu
            +menu({
                items,
                theme: "vertical",
            })
