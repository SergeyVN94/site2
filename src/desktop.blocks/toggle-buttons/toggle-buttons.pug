include /toggle-button/toggle-button.pug

mixin toggle-buttons({ items, head })
    .toggle-buttons
        if head
            .toggle-buttons__head
                +block-head(head)
        .toggle-buttons__container
            for item in items
                .toggle-buttons__item
                    +toggle-button(item)
