mixin radio-button({ text, group = false, checked = false })
    label.radio-button
        input.radio-button__input(type="radio", name=group, checked=checked)
        .radio-button__new-input
        p.radio-button__text #{text}
