include /block-head/block-head.pug
include /checkbox-button/checkbox-button.pug

mixin checkbox-list({ items, head, isRichButtons })
    .checkbox-list(class=(isRichButtons ? "checkbox-list_rich-buttons" : ""))
        if head
            .checkbox-list__head
                +block-head(head)
        ul.checkbox-list__container
            for item in items
                li.checkbox-list__item
                    +checkbox-button(item)
