mixin text-field(options = {})
  -
    const {
      title,
      titleTagNumber = 3,
      withArrow,
      mask = false,
      type = "text",
      placeholder = false,
      value = false,
      name = false,
    } = options;

  .text-field(class=(withArrow ? "text-field_with-arrow" : ""))
    #{`h${titleTagNumber}`}.text-field__title #{title}
    .text-field__wrapper
      input.text-field__input.js-text-field__input(
        type=type,
        placeholder=placeholder,
        value=value,
        data-mask=mask,
        name=name
      )
      if withArrow
        .text-field__arrow
          +button({
            text: "arrow_forward",
            theme: "icon",
            type: "submit",
            textColor: "gradient-primary",
          })    
