mixin input-checkbox(options={})
    -const {
    -   checked = false,
    -} = options;

    label.input-checkbox
        input.input-checkbox__input(type='checkbox', checked=checked)
        .input-checkbox__new-input check